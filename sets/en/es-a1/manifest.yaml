# Per-set manifest — the template's one example set.
#
# Replace every field with your own. The content loader reads the file
# list from `metadata.lessons` (one entry per lesson JSON in `lessons/`).
schema_version: '1.5'
name: Example set — Spanish A1
description: A one-lesson example set. Replace it with your own content.
sets:
  - id: example-set
    title: Spanish A1 (example)
    title_native: Español A1 (ejemplo)
    target_language: es
    source_language: en
    domain: language
    level: A1
    path: sets/en/es-a1
    version: '1.0.0'
    lesson_count: 1
    tags:
      - example
      - spanish
      - a1
metadata:
  author: Your Name
  license: CC-BY-SA-4.0
  lessons:
    - 01-example.json
